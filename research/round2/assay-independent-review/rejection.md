# Independent rejection of the current assay-review implementation

**Do not pursue under rubric v2: the preregistered distinctive-advantage hard gate failed.** At the fixed 20% budget, reviewing 10 of 50 held-out images, the candidate corrects 6 of 18 wrong proxy decisions. A simple threshold-margin ranking corrects 7. Remaining errors are 12 versus 11, whereas the gate required candidate remaining errors at most 8.8 and at least three extra corrections. This is a substantive failure against a cheap credible baseline, not insufficient error opportunity: an oracle could correct 13.

The candidate beats the validation-selected uncertainty baseline (6 versus 2 corrected), but the protocol requires beating every baseline. It also fails the stochastic comparators: random's 95th-percentile benefit is 6.05 and stratified review's is 6. The candidate's extra corrections over their means are only 2.406 and 2.468, below three. Results at 30% are descriptive and cannot replace the frozen 20% gate.

I read PROTOCOL.md, rank.py, calibrate.py, evaluate.py and README.md, inspected frozen results and independently recomputed the deterministic correction totals and oracle opportunity from per-image errors and ranking indices. The ranking SHA256 matches the supplied freeze: e9bac94c707544492512ac3d0d28ababe653f9495cdaae3d88f4efd77515dbc0. Protocol hash also matches its recorded freeze. Audit numbers and hashes are in result-audit.json. I did not independently rerun segmentation or decode annotations; that limitation does not affect the decisive arithmetic rejection on the supplied evidence.

Selection code uses image-derived predictions and training/validation calibration, not test-mask input; evaluator alone reads test masks. Code implements the stated primary gate, including a failure if a baseline has zero remaining errors. The protocol's equal-cost perfect review and nuclear-quantile proxy decisions remain narrow assumptions. No actual biological, clinical or labor-saving result is established.

The prior commercial memo remains relevant: paid analyst work exists, and a decision-sensitive review queue is a plausible practical combination, but deepflash2, SPACe, coSMicQC, CellProfiler Analyst, Genedata and recent downstream-aware clinical QC constrain differentiation. They do not rescue a failed local advantage test. Preserve this negative result; any new method or test would need a new protocol and must not overwrite it. No numerical pursuit score is necessary after this failed hard gate.
