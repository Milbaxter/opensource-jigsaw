# Round seven: engineering acceptance workflows

No pursuit pass. [Three bounded screens](engineering/SCREEN.md) examine engineering simulation checkpoints, document export semantics, and analog diagnostic probe selection. None has an executed comparative result. Each names existing approaches and the unresolved input or advantage gap.

FMU lifecycle acceptance is the strongest reserve, under source-level review. Existing FMPy tests and published metamorphic testing already cover basic rollback; a useful integration must demonstrate more than those checks. Historical issue reports are inspected leads, not blind failures or licensed model fixtures. The request for two failure/correction pairs in the screen is the researcher's proposed study scope, not an additional rubric-v2 requirement.
