# Exact rights and provenance — prospective native screen

The benchmark does not redistribute compiled binaries or a FreeCAD application.

- FreeCAD Adaptive.cpp and Adaptive.hpp at commit `691a041981b7972d3170ceadb96163019712d2b0` have SPDX LGPL-2.1-or-later. Their older prose also permits an earlier LGPL version; this experiment uses the explicit LGPL-2.1-or-later declaration. The source preparation preserves complete upstream copyright/license headers. The small C++ harness is offered under LGPL-2.1-or-later. Modified source and build instructions are available; no claim of an MIT-only binary is made.
- Bundled Clipper1 is Angus Johnson's6.4.2 with BSL-1.0 header and copyright/attribution. Bundled Clipper2 is version2.0.1, with its full Boost license in `src/3rdParty/Clipper2/LICENSE`. Exact19 fetched files, including version template, URLs and hashes are in native-source/manifest.json. Source headers retain their original attributions.
- Original Python orchestration, fixture extraction, source fetch/preparation and rational geometry helpers are MIT licensed; LICENSE-MIT.txt is included. This does not relicense any upstream component.
- The actual `test_adaptive.fcstd` embedded document metadata specifies CC BY4.0, `https://creativecommons.org/licenses/by/4.0/`. Source: FreeCAD contributors, LastModifiedBy Russell Johnson; exact fixture SHA256 `34207b128f433c53b68f09e58a14412eac6eff36bb6f25c7baaff659bf5d7375`. Derived planar footprints are adapted from its box/cut/fuse geometry; changes are explicit kernel operation configurations and planar projection, not a reproduced saved CAM program. Preserve this attribution with derived inputs/results. The original fixture need not be republished; the pinned public URL appears in kernel-inputs.json.
- C++ standard library/compiler runtime and Python standard library are used locally. No Python geometry/optimization dependency or its wheel is added. No Detour code is part of this native-only screen. A later actual Detour build would need its own exact pin/audit; today's general Zlib project declaration is not that audit.

The private source-freeze record includes exact local binary hashes for execution provenance. The public packet includes those hashes, original source URLs/hashes, source preparation and original harness; it excludes binaries, full upstream source copies and unrelated private paths. Compilation feasibility is not path-generation evidence.
