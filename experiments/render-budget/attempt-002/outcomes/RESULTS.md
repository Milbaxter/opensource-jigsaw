# Attempt002: byte-bound guard stopped before candidate rendering

Freeze9d0a19c9793276cdfae1185421c520df997609d3. The unmodified13-source snapshot verified330 retained-file hashes and passed all fresh authored controls. FlightHelmet's canonical renderer repeated and source-to-GLB raw-RGBA comparisons were exactly equal in all six views. This resolves the amended control on this asset only; no quality advantage was established.

All18 actual SpatialPack FlightHelmet recipes completed in198.491 seconds, with no recipe/semantic failure. Every output exceeded the maximum5,000,000-byte ceiling; the smallest was5,459,160 bytes (mobile512KTX). Hence no comparator quality render was attempted. The complete actual recipe reports and output-size/hash manifest are retained, together with the smallest actual output as a representative artifact. A native duplicate Objective-C class warning from the two pinned Sharp/libvips versions is recorded, not silently suppressed or repaired.

FlightHelmet1MB and2MB were recorded as infeasible within our fixed writer family. Before the first5MB preset could render, the exact serialized-file guard raised **Serialized conservative size bound violated**. The process exited1 after220.883 seconds actual wall time, with574.492 seconds of original preprocessing charged, for795.375 effective seconds. No silent retry, tolerance change or implementation repair occurred during the attempt.

There were **zero allocation configuration renders, zero comparator quality renders, no selection seal and zero held-out evaluations**. Retained preparation already contains279 individual image-domain distortion values and option sizes; these were not newly discovered quality wins. The exact exposure ledger is attempts/attempt-002/EXPOSURE.json. The observed wall time of attempts001+002 totals802.707 seconds, excluding the separately disclosed diagnostic whose exact time was not instrumented.

Two issues require prospective review before another attempt. First, static primary-source inspection shows that glTF Transform4.5.0 pads the absolute cumulative binary offset to8bytes after every image, whereas our frozen bound/CPcosts used4-byte image padding. Simply replacing round4 withround8 while retaining the old residual is also unsound for a first-image prefix4mod8; FlightHelmet and WaterBottle have that layout. A corrected proof must derive the actual fixed prefix/tail and audit all padding residues. JSON/chunk alignment remains4.

Second, the frozen summary required five held-out method rows even when the complete comparator frontier has no in-budget output. That conflates an evaluated-infeasible baseline with a missing or failed comparator. The original primary text instead refers to the strongest feasible baseline. This discrepancy is under independent prospective review; the current failed attempt is not rescored or called a pass.

Original execution, source and output artifacts are preserved unchanged. outcomes-002-publish-manifest.json explicitly maps safe copies, including raw/transformed hashes and local-root redaction. Do not recursively copy the work directory or publish restricted comparator implementation. Any third attempt needs a new published freeze and preserves both prior failures.
